# T-102: Секретные чаты — транспорт (слепой релей) + локальная история

> Сверка 2026-09-15: Релей и history реализованы, но ratchet/history сохраняются отдельно; окно потери сообщения при сбое остаётся открытым (audit-fixes.md, №5). Текущий Android DEK требует ещё не реализованного Keystore. Июньские проверки ниже исторические. См. [текущий этап](../.claude/CURRENT_PHASE.md) и [Android](../docs/MOBILE.md).

**Phase:** 6 — Мобильный клиент / Секретные чаты
**Model:** Opus 4.7 · `high` — **SECURITY, корректность слепого релея.**
**Estimate:** ~5-6 часов

## Статус (2026-06-27)

**Сделано — серверная часть (слепой релей):**
- Таблица `secret_envelopes` (`ciphertext bytea`, без колонок content/text) — миграция drizzle `0021`.
- `routes/secret-chat.ts`: `POST /api/secret/send` (+ WS `secret.envelope` без контента), `GET /api/secret/inbox`, `POST /api/secret/ack`.
- `lib/secret-sweeper.ts`: retention недоставленных конвертов > 30 дней.
- WS-событие `secret.envelope` в `ginzu/ws-events.ts` (union + schema). `pnpm typecheck` зелёный.

**Сделано — устройство + pipeline (реализация 2026-06-27):**
- **Зашифрованная локальная история** `src-tauri/src/store/local_db.rs` (`HistoryStore`): снапшот сообщений (id/peer/direction/body/sentAt/readAt/status), зашифрован AES-256-GCM через общий `crate::sealed` тем же DEK, что и крипто-стор. Атомарная запись. Rust-тесты (append/list/read-receipt + reopen) зелёные.
- **Общий слой at-rest** `src-tauri/src/sealed.rs` + `src-tauri/src/error.rs` — вынесены из крипто-стора T-101 (KeyProvider/DEK/seal/unseal/atomic write), теперь шарятся крипто-стором и историей.
- **Команды** `commands.rs`: `secret_history_append_outgoing/incoming`, `secret_history_mark_read`, `secret_history_list`, `secret_history_peers` (managed `HistoryState`).
- **host-абстракция** `lib/host/secret-store.ts` (web-путь бросает `secret-chats-unsupported`).
- **Pipeline** `features/secret/api.ts`: `sendSecretText` / `sendReadReceipt` / `sendTyping` (шифрует frame → POST /secret/send → пишет локальную копию), `drainOnce`+`startSecretChatListener` (WS `secret.envelope`/`ready` → последовательный слив inbox → расшифровка → история → ack; повторные сливы сериализованы ради порядка ratchet), `initSecretChats`/`publishKeys`/`topUpKeys` (публикация бандла в каталог, долив при нехватке).

**Решение по протоколу контрол-сообщений (отступление от подсказки #9):** read/typing — это НЕ отдельные значения серверного `msgType`, а зашифрованный **`SecretFrame`** ВНУТРИ конверта (`ginzu`: discriminated union text/read/typing). Серверный `msgType` остаётся типом ciphertext'а libsignal (`prekey`/`message`). Так сервер не видит даже характер трафика — это сильнее подсказки и совпадает с комментарием в схеме `secretMsgTypeEnum`.

**Решение по хранилищу (отступление от подсказки SQLCipher):** локальная история — единый зашифрованный снапшот через `crate::sealed`, НЕ SQLite/SQLCipher. Причина: SQLCipher тянет C/boringssl и ломает pure-Rust кросс-компиляцию под Android (ради которой и выбрана связка libsignal). Карточка допускает «либо app-private storage». Масштаб (15-20 друзей, текст) делает rewrite снапшота дешёвым.

**Остаток (нативный, не верифицируется без эмулятора):** ключ истории = тот же DEK из `SoftwareKeyProvider` (T-101). Аппаратное запечатывание — Android Keystore (общий с T-101 шаг). Прогон A→B на двух живых эмуляторах — ручной шаг (вместе с UI T-103).

## Контекст

Секретным чатам нужен транспорт, который сервер **не может прочитать**, плюс **локальная история на устройстве** (device-bound → сервер не хранит плейнтекст, история живёт только на телефоне). Сервер работает как **store-and-forward очередь шифр-конвертов**, удаляя их после доставки (модель Signal). Это **параллельный** путь к обычным cloud-DM из T-064 — обычные DM не трогаем.

Завязано на T-101 (crypto_encrypt/decrypt) и архитектурное правило: **все мутации через REST, WebSocket — только receive-only** уведомление «тебе письмо».

Читать: `tasks/T-101.md`, `tasks/T-064.md` (DM, `assertCanAccessChannel`), `ARCHITECTURE.md` §data flow, `packages/ginzu/src/ws-events.ts`.

## Файлы в скоупе

- `packages/speedy/src/db/schema.ts` (таблица `secret_envelopes` — очередь шифр-конвертов)
- `packages/speedy/src/routes/secret-chat.ts` (новый — send / inbox / ack)
- `packages/speedy/drizzle/000X_secret_envelopes.sql` (миграция)
- `packages/ginzu/src/ws-events.ts` (событие `secret.envelope`)
- `packages/ginzu/src/api-types.ts` (`SecretEnvelope`, `SecretMsgType`)
- `packages/polly/src-tauri/src/store/local_db.rs` (новый — зашифрованная SQLite истории)
- `packages/polly/src/lib/host/secret-store.ts` (новый — host-абстракция над локальной историей)
- `packages/polly/src/features/secret/api.ts` (новый — send/receive pipeline)

## Что сделать

### Сервер — слепая очередь (`speedy`)

1. **Таблица `secret_envelopes`**: `id uuid pk, fromUserId fk, toUserId fk, ciphertext bytea, msgType smallint, createdAt`. **Нет колонки content/text.** Индекс `(toUserId, id)`. `ciphertext` — `bytea`, не `text` (его нельзя гонять через tsvector).
2. **Эндпоинты** (`secret-chat.ts`, authenticated, Zod):
   - `POST /api/secret/send` — `{ toUserId, ciphertext (base64), msgType }`. Сервер кладёт **непрозрачный** blob, **не парсит**. Если получатель онлайн — шлёт WS `secret.envelope`.
   - `GET /api/secret/inbox` — pending-конверты для меня.
   - `POST /api/secret/ack` — `{ ids: [] }` — удалить доставленные. После ack blob физически удаляется.
   - **Auth**: отправитель должен иметь право писать получателю — переиспользовать логику доступа из T-064 (общий сервер / разрешённый DM).
3. **Retention**: cron чистит недоставленные конверты старше 30 дней (offline-получатель). Зафиксировать cap в конфиге.
4. **WS** (`secret.envelope`): несёт **только** «есть письмо» + `id`/`fromUserId`, **никакого контента**. Receive-only, как вся WS-архитектура.

### Устройство — локальная история (Rust, `src-tauri`)

5. **Зашифрованная SQLite** (`local_db.rs`): ключ шифрования — wrapping-ключ из T-101 (Keystore). Схема: `messages (id, peerUserId, direction, plaintext, msgType, sentAt, readAt, status)`. Это **источник истины** истории секретных чатов.
6. **Device-bound по-настоящему**: при wipe данных приложения / переустановке локальная БД и ключи исчезают → история секретных чатов теряется. Это ожидаемое поведение, не баг.

### Pipeline (`features/secret/api.ts` + `host/secret-store.ts`)

7. **Отправка**: plaintext → `host.crypto.encrypt(toUserId)` → `POST /api/secret/send` → записать свою копию в локальную БД (`status='sent'`) → инвалидировать query.
8. **Приём**: WS `secret.envelope` → `GET /api/secret/inbox` → по каждому `host.crypto.decrypt(fromUserId)` → запись в локальную БД → `POST /api/secret/ack` → инвалидировать query.
9. **Контрол-сообщения**: read-receipt и typing — это **типизированные конверты** (`msgType='read'` / `'typing'`), идущие тем же E2EE-каналом, **не** отдельным REST. Прочитанность (`✓✓`) ставится по входящему `read`-конверту.

## DoD

- [~] A→B (оба онлайн): шифруется → доходит → расшифровывается у B; строка удаляется после ack — **pipeline собран** (`sendSecretText`→`/secret/send`; `drainOnce`→decrypt→`/secret/ack`). Прогон на двух живых эмуляторах — ручной шаг (с UI T-103).
- [~] B оффлайн → конверт в очереди → доставляется при reconnect — `startSecretChatListener` сливает inbox на WS `ready` (backfill). Ручной прогон — с UI.
- [x] Инспекция серверной БД показывает **только `ciphertext bytea`** (+ `msg_type` = тип libsignal) — плейнтекста нет by design; вид сообщения зашифрован в `SecretFrame`.
- [x] Локальная история переживает рестарт — Rust-тест `history_survives_reopen` (читается из зашифрованного снапшота).
- [x] Wipe данных → история исчезла — снапшот и DEK лежат в `<app_data>/kd-secret/`, при wipe удаляются (device-bound by design).
- [x] WS `secret.envelope` не содержит контента — payload `{ t, id, fromUserId }` (см. `ws-events.ts`).
- [x] Read-receipt `✓✓` через входящий control-конверт — `read`-frame → `mark_outgoing_read` → `status='read'` (Rust-тест `append_list_and_read_receipt`).
- [x] `pnpm typecheck` + `cargo check` (desktop) + `cargo ndk` (android) — чистые, без warnings.

## Out of scope

- UI секретного чата — T-103. Здесь pipeline + хранилища.
- Самоуничтожающиеся сообщения (таймер) — отдельная задача.
- Вложения/картинки в секретных чатах — пока **только текст**.
- Серверный поиск по секретным чатам — **невозможен by design**, не делаем.
- Мульти-девайс синк, бэкап истории — нет (device-bound).

## Подсказки

- `ciphertext` строго `bytea`, не `text` — иначе кто-нибудь случайно повесит на него GIN/tsvector.
- Обычный DM из T-064 **не трогаем** — секретный чат это параллельная ветка, отдельные таблицы и роуты.
- WS остаётся «глупым звоночком»: контент только в зашифрованном конверте по REST, в WS — лишь факт.
- Зашифрованная локальная БД: `tauri-plugin-sql` + SQLCipher, либо app-private storage. Ключ — из T-101.
- Retention-cron не должен трогать доставленные (их уже нет) — только висящие offline-конверты.
