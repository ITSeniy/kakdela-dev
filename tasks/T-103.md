# T-103: Секретные чаты — мобильный UI + верификация ключей

> Сверка 2026-09-15: UI и верификация реализованы. Живой прогон на Android зависит от Keystore и исправлений сохранности/изоляции хранилища; июньские галочки ниже не закрывают текущие ограничения аудита. См. [текущий этап](../.claude/CURRENT_PHASE.md) и [Android](../docs/MOBILE.md).

**Phase:** 6 — Мобильный клиент / Секретные чаты
**Model:** Sonnet 4.6 · `medium` (UI по дизайну; safety-number логика — внимательно)
**Estimate:** ~5-6 часов

## Статус (реализовано 2026-06-27)

**Сделано (UI + верификация, проверено typecheck + web build):**
- Иконки `Lock`/`ShieldCheck`/`Check`/`CheckCheck` в `components/Icon.tsx`.
- `features/secret/`: `SecretBubble`, `SecretChatScreen` (шапка с замком/presence/verified-бейджем, баннеры, лента из локальной истории, composer text-only, read-receipt, typing-индикатор, блок отправки при смене ключа), `SecretChatList`, `StartSecretChat` (кнопка в `ProfileModal`, mobile-only), `KeyVerification` (safety number 60 цифр × группы по 5, mono), `SessionBanner` (protected/establishing/key-changed/device-bound), `sessionStore` (эфемерные keyChanged/typing), `verifyStore` (verified safety number + onboarding через at-rest `secrets`).
- `MobileShell`: роуты `/secret` (список) и `/secret/:userId` (экран), вход «секретные чаты» в табе чатов, bootstrap `initSecretChats` + `startSecretChatListener` (mobile-only).
- Крипто-ядро T-101 дополнено для key-change flow: `crypto_clear_session` (+ host `cryptoClearSession`), а `error.rs` выделяет `untrusted-identity` отдельным кодом (детект смены ключа на расшифровке). `cargo test --lib` (4) + desktop/android check зелёные.

**Дизайн-референс (актуализировано):** макет НАШЁЛСЯ в Claude Design (проект `7d516a7f-…`), в подкаталоге `designs/`: **`final-secret-chat.jsx`** (отдельные экраны: SecretChatList, SecretChatScreen, KeyVerification, SessionStates) и **`final-mobile.jsx`** (полное мобильное приложение, секретный чат внутри). Локально замиррорен `designs/final-mobile.jsx`. UI **приравнен к макету** (2026-06-27, повторный проход):
- SecretBubble — аватар 26 у входящих, футер mono с замком + «✓✓ прочитано» зелёным.
- SecretChatScreen — шапка (аватар+статус, имя 15px, пилюля «проверено», подпись «🔒 секретный · только на этом устройстве», иконка «···»), системный баннер, composer с круглой accent-кнопкой и футером «🔒 E2EE · device-bound».
- KeyVerification — контакт-аватар 56, код 60 цифр (12×5, 18px mono), QR-плейсхолдер, полноширинная кнопка.
- SessionBanner — establishing (спиннер + «обмен ключами · x3dh · double ratchet»), key-changed (danger-тинт + Alert + «отправка заблокирована»), device-bound (warm icon-box + mono-футер).
- SecretChatList — шапка «секретные чаты · N переписок · сквозное шифрование», строка поиска, строки avatar 46 + 🔒 + «проверено» + unread-бейдж + предупреждение о смене ключа.
- Иконки Alert/More добавлены. Токены не захардкожены.

**Остаток (ручной шаг, не верифицируется без двух эмуляторов):** живой прогон A→B (текст туда-обратно), смена ключа после переустановки, совпадение safety number на двух устройствах. Делается вместе с прогоном T-101/T-102.

## Контекст

Мобильный UI секретных чатов — «как обычный мессенджер», но с замочками и верификацией. Опирается на T-100 (shell), T-101 (крипто), T-102 (транспорт + локальная история). Дизайн-референс: **`designs/final-secret-chat.jsx`** (генерируется по промпту для Claude Design — см. ниже по тексту обсуждения / тред фазы 6). Визуальный язык — тёплая бежевая палитра, как в `designs/final-dm.jsx`.

Читать: `designs/final-secret-chat.jsx`, `designs/final-dm.jsx` (`KD_DMBubble`, `KD_DMList`), `.claude/CONVENTIONS.md` (Дизайн-токены), `tasks/T-101.md` (`crypto_safety_number`), `tasks/T-102.md` (pipeline).

## Файлы в скоупе

- `packages/polly/src/features/secret/SecretChatList.tsx` (новый)
- `packages/polly/src/features/secret/SecretChatScreen.tsx` (новый)
- `packages/polly/src/features/secret/SecretBubble.tsx` (новый)
- `packages/polly/src/features/secret/StartSecretChat.tsx` (новый)
- `packages/polly/src/features/secret/KeyVerification.tsx` (новый — safety number)
- `packages/polly/src/features/secret/SessionBanner.tsx` (новый — establishing / key-changed / device-bound)
- `packages/polly/src/app/MobileShell.tsx` (подключить роут секретных чатов)
- `packages/polly/src/lib/host/crypto.ts` (использование safety number / verified-флага)

## Что сделать

1. **Список секретных чатов** (`SecretChatList`):
   - Источник — локальная история (T-102), не сервер.
   - Строка: avatar + имя + превью последнего сообщения (из локального плейнтекста) + время + unread + **🔒-бейдж**.
   - Online-точка из presence (Redis/WS) — это **несекретные** метаданные, использовать можно.
   - Если показываем рядом cloud-DM — визуально отделить (замок vs без).
2. **Экран чата** (`SecretChatScreen`):
   - Шапка: `🔒 секретный · только на этом устройстве`, имя, presence.
   - Системный баннер сверху: «сообщения защищены сквозным шифрованием. история хранится только здесь».
   - `KD_DayDivider`, пузыри `SecretBubble`, composer (**пока только текст**).
   - **Без link-preview / без серверных эмбедов** (приватность + рендер локальный). Markdown-форматирование текста — можно, через существующий `markdown-it` + DOMPurify, но **без** запроса OG-превью.
3. **Пузырь** (`SecretBubble`):
   - Базируется на стиле `KD_DMBubble` (мои справа — `accent`/белый текст; чужие слева — `panel` + border).
   - Микро-иконка замка + статус `✓✓ прочитано` (по T-102 read-конверту).
4. **Старт секретного чата** (`StartSecretChat`):
   - Из профиля пользователя кнопка «начать секретный чат» → `crypto_session_exists?` → если нет: `GET /api/keys/:userId/bundle` (T-101) → установить сессию → открыть экран в состоянии «устанавливаем защищённое соединение…».
5. **Верификация** (`KeyVerification`):
   - Показать safety number (`crypto_safety_number`) сгруппированными mono-цифрами (стиль Signal: 60 цифр группами по 5).
   - Копия «сравните этот код с <имя> лично».
   - Кнопка «отметить проверенным» → флаг хранится **локально** (device-bound, не на сервере) → бейдж «проверено» в шапке.
6. **Баннеры сессии** (`SessionBanner`):
   - «ключ безопасности <имя> изменился — проверьте заново» при смене identity-ключа собеседника (libsignal untrusted identity) → **блокировать отправку** до подтверждения.
   - Device-bound онбординг-карточка при первом секретном чате: «история хранится только на этом телефоне. потеряете устройство — переписка исчезнет».
7. **Read-receipt / typing**: подключить к control-конвертам из T-102.

## DoD

- [~] Из профиля «начать секретный чат» → сессия устанавливается → текст туда-обратно — UI + pipeline собраны (`StartSecretChat`→`/secret/:id`→`ensureSecretSession`→`sendSecretText`). Живой round-trip на двух эмуляторах — ручной шаг.
- [x] 🔒-индикаторы на месте; секретный чат и cloud-DM визуально различимы (warm-замок vs обычный DM; отдельный список и вход).
- [~] Safety number одинаков на обоих устройствах (детерминизм доказан Rust-тестом T-101); бейдж «проверено» переживает рестарт — verified SN хранится в at-rest `secrets`. Сверка двух устройств — ручной шаг.
- [x] Смена identity собеседника → баннер «ключ изменился» + блок отправки до re-verify — детект `untrusted-identity` на расшифровке → `flagKeyChanged` → `KeyChangedBanner` + disabled composer; re-verify = `crypto_clear_session` + переобработка конверта.
- [x] Device-bound онбординг показан один раз (`verifyStore.hasSeenOnboarding`/`markOnboardingSeen`).
- [x] Соответствует `designs/final-secret-chat.jsx` / `final-mobile.jsx` (Claude Design, замиррорен локально) — UI приравнен к макету; токены не захардкожены.
- [x] `pnpm typecheck` чистый (+ web `vite build` зелёный, `cargo test`/`cargo ndk` зелёные).

## Out of scope

- Самоуничтожающиеся сообщения (UI таймера) — отдельная задача.
- Вложения / голосовые в секретных чатах — нет (T-102: только текст).
- Групповые секретные чаты — нет (1:1).
- Секретные чаты на desktop — нет (мобилка-only, device-bound).
- QR-верификация — позже; пока сравнение цифрами хватает.

## Подсказки

- Presence/online — несекретные метаданные через существующий WS, использовать спокойно.
- Плейнтекст рендерить существующим пайплайном `markdown-it` + DOMPurify, но **выключить** fetch OG-превью для секретных.
- Формат safety number — как у Signal: 60 цифр, группы по 5, mono-шрифт (`--kd-mono`).
- Флаг «проверено» — **только локально**, на сервер не отправлять (device-bound).
- `SecretBubble` переиспользует геометрию `KD_DMBubble`, добавляя замочек; не копировать стили хардкодом — через токены.
- «Ключ изменился» — это сигнал безопасности, не косметика: блок отправки обязателен, иначе теряется смысл E2EE.
