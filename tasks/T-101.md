# T-101: Крипто-ядро — libsignal в Rust + ключи (device-bound)

> Сверка 2026-09-15: Текущий sealed.rs требует OS keychain и на Android возвращает keychain-unavailable. Описанный ниже software DEK не является рабочим production fallback. Keystore, namespace и восстановление остаются открытыми в audit-fixes.md (№4/17). См. [текущий этап](../.claude/CURRENT_PHASE.md) и [Android](../docs/MOBILE.md).

**Phase:** 6 — Мобильный клиент / Секретные чаты
**Model:** Opus 4.7 · `high` — **SECURITY, крипто. Не импровизировать.**
**Estimate:** ~6-8 часов

## Статус (частично, 2026-06-27)

**Сделано — серверная часть (слепой каталог ключей), без нативного тулчейна:**
- Таблицы `secret_identities` + `secret_one_time_prekeys` (только публичные ключи) — миграция drizzle `0021`.
- `routes/keys.ts`: `POST /api/keys/bundle`, `POST /api/keys/topup`, `GET /api/keys/count`, `GET /api/keys/:userId/bundle` (атомарная выдача one-time prekey через `FOR UPDATE SKIP LOCKED` в транзакции).
- Схемы бандла в `ginzu/api-types.ts`. `pnpm typecheck` зелёный.

**Заблокировано тулчейном** (Rust/libsignal в `src-tauri`, Android Keystore, host/crypto.ts) — делается вместе с нативной частью T-100. Тулчейн установлен 2026-06-27 (APK собирается, см. `docs/MOBILE.md`) → можно начинать.

**В скоуп добавлен нативный Keystore-мост (свёрнут из T-100):** один Kotlin-плагин (EncryptedSharedPreferences, Keystore-backed master key) + Rust-команды обслуживают и sealed identity-key libsignal, и (опционально) перенос access-токена из софтварного стора `lib/host/secrets.ts`. Строим один раз здесь.

### Прогресс реализации (2026-06-27)

**Фундамент готов — libsignal подключён и кросс-компилируется:**
- `src-tauri/Cargo.toml`: `libsignal-protocol = { git, tag = "v0.96.4" }`. Не на crates.io → пин на git-тег.
- `cargo check` **desktop** — зелёный; `cargo ndk -t x86_64 --platform 24 check` — **зелёный** (Android-кросс работает: curve25519-dalek + ядро на чистом Rust, без boringssl/C-проблем).
- Новое требование тулчейна: **protoc** (build-скрипты libsignal). Задокументировано в `docs/MOBILE.md`.

### ⚠ Решение по протоколу: PQXDH, не классический X3DH (2026-06-27)

libsignal v0.96.4 **не даёт** установить сессию классическим X3DH: `PreKeyBundle::new`
требует обязательный Kyber prekey, `process_prekey_bundle` всегда проверяет его подпись.
Версия реализует **PQXDH** (post-quantum X3DH: X25519 + Kyber1024). Понизить версию
рискованно — именно v0.96.4 чисто кросс-компилируется под Android (старые теги тянут
boringssl/C). Согласовано с юзером → **Path A: принимаем PQXDH** (это строго сильнее).
Последствия:
- Карточка изначально говорила «X3DH» и «post-quantum — нет»; фактически здесь PQXDH.
  Сам обмен сообщениями (Double Ratchet) не меняется.
- В каталог добавлен **подписанный Kyber prekey** (один на пользователя, last-resort):
  столбцы `kyber_pre_key*` в `secret_identities` (миграция **0022**), поля `kyberPrekey`
  в ginzu (`PublishKeysRequest`/`PrekeyBundleResponse`), запись/выдача в `keys.ts`.

**Ядро реализовано и протестировано:**
- `crypto/store.rs` — стора Identity/Session/PreKey/SignedPreKey/**Kyber** libsignal поверх
  AES-256-GCM снапшота (атомарная запись tmp+rename). Каждый трейт — на своём суб-сторе
  (непересекающиеся `&mut` для session+identity в `message_encrypt`).
- `crypto/mod.rs` — identity, signed+kyber+one-time prekeys, `process_bundle` (PQXDH),
  encrypt/decrypt, симметричный safety number (`Fingerprint`, v2, 5200 итераций).
- `commands.rs` + регистрация в `lib.rs` (managed `Mutex<Option<CryptoCore>>`).
- `host/crypto.ts` — обёртка; web-путь бросает `secret-chats-unsupported`.
- **Тесты** (`cargo test --lib crypto`, оба зелёные): PQXDH+ratchet round-trip между двумя
  «устройствами» + симметрия safety number; переживание перезапуска (reopen стора).

**Осталось (нативный шаг, не верифицируется в этой сессии без Kotlin/эмулятора):**
аппаратное запечатывание DEK через **Android Keystore**. Сейчас DEK даёт
`SoftwareKeyProvider` (ключ файлом рядом со стором — программный уровень, как
`secrets.ts`). Keystore-провайдер подменяет ТОЛЬКО `KeyProvider` в `store.rs`, ядро не
трогает. Сюда же сворачивается hardware-хранилище JWT из T-100. Детали — `docs/MOBILE.md`.

## Контекст

Сердце E2EE. `libsignal` (ядро на Rust) линкуется в `src-tauri` и работает на нативной стороне — WebView ключей **не видит**. Протокол: **X3DH** для установки сессии + **Double Ratchet** для сообщений (forward secrecy из коробки). Это стандартный, проверенный 1:1-протокол Signal/WhatsApp/секретных чатов Telegram — мы его **используем**, а не переизобретаем.

**Device-bound** (решение фазы 6): identity-ключ генерируется на устройстве, приватная часть запечатана Android Keystore, **никогда** не покидает телефон и **не синхронизируется**. Потеря устройства = потеря секретных переписок. Это by design.

Сервер выступает **слепым каталогом ключей** (key directory): хранит только **публичные** prekey-бандлы, не может расшифровать ничего. Это единственная серверная часть этой карточки.

Читать: `.claude/CONVENTIONS.md` (Безопасность; «нет секретов в логах»), `tasks/T-100.md` (host-layer), документацию libsignal (X3DH, Double Ratchet, prekey bundle).

## Файлы в скоупе

- `packages/polly/src-tauri/Cargo.toml` (зависимость на libsignal — pin git rev / vendor)
- `packages/polly/src-tauri/src/crypto/mod.rs` (новый — identity, prekeys, session, ratchet)
- `packages/polly/src-tauri/src/crypto/store.rs` (новый — зашифрованный стор сессий/identity, ключ из Keystore)
- `packages/polly/src-tauri/src/commands.rs` (tauri-команды, экспонируемые в JS)
- `packages/polly/src/lib/host/crypto.ts` (новый — host-абстракция; web-mock бросает `secret-chats-unsupported`)
- `packages/speedy/src/db/schema.ts` (таблицы prekey-каталога)
- `packages/speedy/src/routes/keys.ts` (новый — каталог ключей)
- `packages/speedy/drizzle/000X_keys.sql` (миграция)
- `packages/ginzu/src/api-types.ts` (`PrekeyBundle`, `IdentityKey`, `SignedPrekey`, `OneTimePrekey`)

## Что сделать

### Нативная сторона (Rust, `src-tauri`)

1. **Identity**: при первом запуске — сгенерировать identity keypair (по схеме libsignal) + `registrationId`. Приватный ключ хранить запечатанным: в Keystore лежит **wrapping-ключ** (StrongBox если доступен), им шифруется локальный стор (Keystore не хранит произвольные байты — только ключ-обёртку).
2. **Prekeys**: сгенерировать signed prekey + N (например 100) one-time prekeys. Наружу отдаются **только публичные** части.
3. **Стор** (`store.rs`): реализовать `IdentityKeyStore` / `SessionStore` / `PreKeyStore` / `SignedPreKeyStore` libsignal поверх зашифрованного локального хранилища. Источник истины сессий — устройство.
4. **Команды** (`commands.rs` → JS):
   - `crypto_init()` — идемпотентно создаёт identity при первом вызове.
   - `crypto_publish_keys()` — формирует публичный бандл для отправки на сервер.
   - `crypto_encrypt(toUserId, plaintext) -> { ciphertext, msgType }` — X3DH при первом сообщении, дальше Double Ratchet.
   - `crypto_decrypt(fromUserId, ciphertext, msgType) -> plaintext`.
   - `crypto_session_exists(userId) -> bool`.
   - `crypto_safety_number(userId) -> string` — детерминированный, **симметричный** (одинаковый у обеих сторон).

### Серверная сторона (слепой каталог, `speedy`)

5. **Таблицы**: `identity_keys (userId pk, identityPubKey, registrationId)`, `signed_prekeys (userId, keyId, pubKey, signature)`, `one_time_prekeys (userId, keyId, pubKey, consumedAt)`. **Только публичные ключи. Приватных в БД нет и быть не может.**
6. **Эндпоинты** (`keys.ts`, все authenticated, Zod на каждом):
   - `POST /api/keys/bundle` — загрузить свой публичный бандл (identity + signed prekey + пачка one-time).
   - `GET /api/keys/:userId/bundle` — получить бандл для старта сессии; сервер **отдаёт и помечает consumed** один one-time prekey (atomic).
   - `POST /api/keys/topup` — долить one-time prekeys, когда осталось мало.
   - `GET /api/keys/count` — сколько one-time осталось (для триггера topup на клиенте).

### Host-абстракция

7. `host/crypto.ts` оборачивает команды; на web-пути (`pnpm dev:web`) бросает `{ code: 'secret-chats-unsupported' }` — секретные чаты только на мобиле.

## DoD

- [x] Чистая установка генерирует identity + бандл, на сервере появляются **только публичные** ключи (`crypto::create` + `publish_keys`; в бандле и БД только публичные части)
- [x] `crypto_encrypt` → `crypto_decrypt` round-trip строки между двумя user'ами — `cargo test --lib crypto` (две независимые `CryptoCore` = два «устройства»). **Прогон на двух живых эмуляторах остаётся ручным шагом.**
- [x] Приватные ключи не покидают нативную сторону: в публичный бандл и REST уходят только публичные части (по конструкции `public_bundle`); base64-ввод не логируется
- [x] `GET /api/keys/:userId/bundle` атомарно консьюмит один one-time prekey (`FOR UPDATE SKIP LOCKED` в транзакции); `topup` восполняет
- [x] `crypto_safety_number` детерминирован и одинаков у обеих сторон (тест `pqxdh_roundtrip_and_safety_number` сверяет обе стороны)
- [x] В логах сервера и клиента **нет** секретного материала (нет `dbg!`/`tracing`/`println!` приватных байт; ошибки описывают тип, не ключи)
- [x] `pnpm typecheck` + `cargo check` (desktop) + `cargo ndk -t x86_64 check` (android) — чистые
- [ ] **Остаток:** аппаратный Android Keystore для DEK (Kotlin-плагин) — нативный шаг, не верифицируется без эмулятора (см. «Осталось» выше)

## Out of scope

- Групповые сессии (MLS) — нет, мобилка 1:1.
- Мульти-девайс / синк ключей между телефоном и desktop — нет (device-bound).
- Бэкап/восстановление ключей — нет (device-bound = потеря устройства = потеря истории, by design).
- Post-quantum, sealed sender — нет.
- Сам обмен сообщениями (envelope queue) — это T-102. Здесь только крипто-примитивы + каталог ключей.

## Подсказки

- Rust-ядро libsignal **не публикуется** как стабильный crate — пин на конкретный git-rev либо vendoring; обновлять осознанно.
- Сборка под Android — `cargo-ndk` (см. `docs/MOBILE.md` из T-100).
- **Не писать свою крипту.** Использовать session-API libsignal как есть. Любое «оптимизирую сам» в этой карточке — красный флаг.
- Keystore хранит маленький wrapping-ключ, а не сессии целиком; сессии — в зашифрованном локальном сторе.
- `registrationId` и id prekey'ев — целые; следить за уникальностью one-time keyId.
- Аккуратно с трейсингом: ни `dbg!`, ни `tracing` не должны проглатывать приватные байты.
